Can you confirm:

Do BOM_LVL and VL both indicate the assembly level in the BOM hierarchy?

Some part numbers have suffixes like +TL, +MP, +PB, etc. — do these suffixes have specific meaning (e.g., variant, revision, plant code), and should we treat them as unique part numbers or strip them for hierarchy mapping?


Could you please share the supplier–component mapping table (just the first 10–20 rows is fine) as a CSV?
Also, could you help me get access to Databricks? I plan to use it to refine our ontology and knowledge graph development workflow.


