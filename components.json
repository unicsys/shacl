import requests
import urllib.parse
import json

# --- Configuration ---
# FIX IS HERE: Corrected the typo in the port number
MOBI_HOST = "https://localhost:8443" 
API_TOKEN = "eyJhbGciOiJIUzI1NiJ9.eyJpc3MiOiJodHRwOi8vbW9iaS5jb20vIiwic3ViIjoiYWRtaW4iLCJleHAiOjE3NjExNDIyMDMsInNjb3BlIjoic2VsZiAvKiJ9._umAkHgoNnAlMqNZioBTPu7rMJW35BnVBqp3pnWxfiM"

# ===================================================================
# >> EDIT THIS SECTION <<
# ===================================================================

# 1. Paste the IRI of the record you want to query.
RECORD_IRI = "https://mobi.com/records#8141a65d-19b0-4a4b-b4cf-e99b548e809f"

# 2. Paste your multi-line SPARQL query between the triple quotes.
SPARQL_QUERY = """
SELECT ?subject ?predicate ?object
WHERE {
  ?subject ?predicate ?object .
}
LIMIT 10
"""

# ===================================================================
# >> END OF EDIT SECTION <<
# ===================================================================

def run_query():
    """Executes the configured SPARQL query against the specified Mobi record."""

    if "your_full_api_token_here" in API_TOKEN or "paste_iri_here" in RECORD_IRI:
        print("Error: Please update API_TOKEN and RECORD_IRI in the script before running.")
        return

    print(f"Querying record: {RECORD_IRI}")

    store_type = "dataset-record"
    encoded_iri = urllib.parse.quote(RECORD_IRI, safe='')
    url = f"{MOBI_HOST}/mobirest/sparql/{store_type}/{encoded_iri}/limited-results"

    headers = {
        'Cookie': f'mobi_web_token={API_TOKEN}',
        'Accept': 'application/json',
        'Content-Type': 'application/x-www-form-urlencoded'
    }
    
    try:
        requests.packages.urllib3.disable_warnings()
        response = requests.post(url, headers=headers, data={'query': SPARQL_QUERY}, verify=False)
        response.raise_for_status()
        
        results = response.json()
        bindings = results.get('results', {}).get('bindings', [])

        if not bindings:
            print("\nQuery successful: No results found.")
            return

        print("\n--- Query Results ---")
        for row in bindings:
            for variable, value in row.items():
                print(f"  {variable}: {value.get('value')}")
            print("-" * 20)

    except requests.exceptions.RequestException as e:
        print(f"\nError during API request: {e}")
        if e.response:
            print(f"Server response: {e.response.text}")

if __name__ == "__main__":
    run_query()
