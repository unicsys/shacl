spark.sql("USE annual_inspection_silver")

tables = [row.tableName for row in spark.sql("SHOW TABLES").collect()]

for t in tables:
    print(f"Schema for table: {t}")
    df = spark.table(f"annual_inspection_silver.{t}")
    df.printSchema()
    print("-" * 40)
